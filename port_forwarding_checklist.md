# Port Forwarding Checklist

## ✅ Проверьте каждый пункт:

### 1. Сервер запущен?
```bash
# Проверить процесс
tasklist | findstr "DedicatedServer"

# Должно быть:
DedicatedServer.exe    24236 Console    1     4,584 K
```
- [ ] Сервер запущен

### 2. Порт открыт локально?
```bash
netstat -an -p UDP | findstr "27015"

# Должно быть:
UDP    0.0.0.0:27015          *:*
```
- [ ] Порт 27015 открыт локально

### 3. Windows Firewall разрешает UDP 27015?
```bash
netsh advfirewall firewall show rule name=all | findstr "27015"

# Должно быть правило с:
Протокол: UDP
Локальный порт: 27015
Действие: Разрешить
```
- [ ] Firewall разрешает входящие UDP на 27015

### 4. Port Forwarding настроен на роутере?

Зайдите в роутер и проверьте:

```
Service Name:     MOBA Server (или любое)
Device IP:        192.168.87.104  ← ВАЖНО!
External Port:    27015
Internal Port:    27015
Protocol:         UDP  ← ВАЖНО! Не TCP, не All
Status:           Enabled ✓
```

**Частые ошибки:**
- ❌ Protocol = "All" или "TCP" (должно быть UDP)
- ❌ Device IP неправильный (должен быть 192.168.87.104)
- ❌ Status = Disabled (должно быть Enabled)

- [ ] Port Forwarding настроен правильно

### 5. Роутер перезагружен после настройки?
- [ ] Роутер перезагружен (Reboot)

### 6. IP адрес не изменился?

```bash
# Проверить локальный IP
ipconfig | findstr "192.168"

# Должно быть:
IPv4-адрес. . . . . . . . . . . . : 192.168.87.104
```

Если IP изменился, нужно:
1. Обновить Port Forwarding на роутере
2. Или настроить Static IP (DHCP Reservation)

- [ ] IP адрес совпадает с настройками роутера

### 7. Провайдер не блокирует порты?

Некоторые провайдеры блокируют входящие соединения или используют CGNAT.

**Признаки CGNAT:**
- Ваш публичный IP начинается с 100.64.x.x
- Port Forwarding не работает вообще
- Роутер показывает WAN IP отличный от публичного

```bash
# Проверить публичный IP
powershell -Command "Invoke-RestMethod -Uri 'https://api.ipify.org'"

# Ваш: 83.99.137.79
```

- [ ] Публичный IP не из диапазона CGNAT (100.64.x.x)

### 8. Антивирус не блокирует?

Проверьте антивирус (Kaspersky, Avast, Norton и т.д.):
- Добавьте DedicatedServer.exe в исключения
- Разрешите сетевую активность

- [ ] Антивирус не блокирует

---

## 🔍 Дополнительная диагностика

### Проверка 1: Локальное подключение работает?

```bash
# На том же ПК
powershell -ExecutionPolicy Bypass -File test_udp_client.ps1 -ServerIP "127.0.0.1"
```

Если НЕ работает → проблема в сервере или firewall
Если работает → проблема в роутере или провайдере

### Проверка 2: Подключение из локальной сети работает?

С другого ПК в вашей сети:
```bash
powershell -ExecutionPolicy Bypass -File test_udp_client.ps1 -ServerIP "192.168.87.104"
```

Если НЕ работает → проблема в Windows Firewall
Если работает → проблема в Port Forwarding или провайдере

### Проверка 3: Подключение извне работает?

Попросите друга запустить:
```bash
powershell -ExecutionPolicy Bypass -File test_external_connection.ps1
```

Если НЕ работает → проблема в Port Forwarding или провайдере
Если работает → всё ОК! 🎉

---

## 🆘 Если ничего не помогает

### Вариант 1: Static IP для вашего ПК

В роутере найдите "DHCP Reservation" или "Static IP":
1. Привяжите MAC адрес вашего ПК к IP 192.168.87.104
2. Это гарантирует, что IP не изменится

### Вариант 2: DMZ (не рекомендуется)

В роутере найдите "DMZ":
1. Включите DMZ
2. Укажите IP: 192.168.87.104
3. Это откроет ВСЕ порты (небезопасно!)

### Вариант 3: UPnP (автоматический Port Forwarding)

В роутере найдите "UPnP":
1. Включите UPnP
2. Сервер сам откроет порт (если поддерживается)

### Вариант 4: VPN (Hamachi, ZeroTier)

Если Port Forwarding не работает:
1. Установите Hamachi или ZeroTier
2. Создайте виртуальную сеть
3. Друзья подключаются через VPN IP

### Вариант 5: Облачный сервер

Арендовать VPS:
- DigitalOcean: $6/месяц
- Hetzner: €4/месяц
- AWS Free Tier: бесплатно 12 месяцев

---

## 📞 Связаться с провайдером

Если ничего не работает, позвоните провайдеру:

**Вопросы:**
1. "Используете ли вы CGNAT?"
2. "Могу ли я получить публичный IP адрес?"
3. "Блокируете ли вы входящие соединения?"
4. "Могу ли я открыть UDP порт 27015?"

Если провайдер использует CGNAT → нужен VPN или VPS.

---

## ✅ Итоговая проверка

Если все пункты выполнены:
- [x] Сервер запущен
- [x] Порт открыт локально
- [x] Firewall разрешает
- [x] Port Forwarding настроен
- [x] Роутер перезагружен
- [x] IP адрес правильный
- [x] Провайдер не блокирует
- [x] Антивирус не блокирует

То порт **должен быть открыт публично**.

Если всё равно не работает → скорее всего проблема в провайдере (CGNAT).
