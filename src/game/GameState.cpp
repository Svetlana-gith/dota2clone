#include "GameState.h"
#include <algorithm>

namespace Game {

void InGameState::Update(f32 deltaTime) {
    // This is now handled by the proper InGameState class
    // The old GameState class is replaced by the new state system
    // This is now handled by the proper InGameState class
    // The old GameState class is replaced by the new state system
}

} // namespace Game
