# Миграция с DX11 на DX12 - План работ

## ✅ Что уже сделано

### 1. Основная инфраструктура DX12
- ✅ DirectXRenderer с полной поддержкой DX12
- ✅ Command queue, command allocators, command lists
- ✅ Swap chain с 3 буферами
- ✅ Descriptor heaps (RTV, DSV, SRV)
- ✅ Fence синхронизация
- ✅ Offscreen render targets для viewport
- ✅ Lighting system, Shadow mapping, Sky renderer, Wireframe grid

### 2. UI система (Panorama)
- ✅ CUIEngine инициализируется с DX12 device
- ✅ CUIRenderer.h определяет интерфейс для DX12
- ✅ **CUIRenderer_DX12.cpp - БАЗОВЫЙ РЕНДЕРИНГ РАБОТАЕТ!**
  - ✅ Root signature создан
  - ✅ Pipeline state создан
  - ✅ Шейдеры скомпилированы
  - ✅ Vertex buffer создан
  - ✅ DrawRect работает
  - ✅ DrawGradientRect работает
  - ✅ DrawLine работает
  - ✅ DrawCircle работает
  - ✅ Transform stack работает
  - ✅ Clipping работает
  - ✅ Alpha blending работает
  - ✅ Batching работает

### 3. Что видно на экране
- ✅ UI рендерится!
- ✅ Панели, кнопки, фоны отображаются
- ✅ Цвета и прозрачность работают
- ✅ Layout система работает
- ❌ Текст не рендерится (показываются placeholder'ы)

## ❌ Что нужно доделать

### 1. **CUIRenderer_DX12.cpp - Полная реализация рендеринга UI**

**Текущее состояние:** Только заглушки (stubs)

**Что нужно реализовать:**

#### A. Инициализация ресурсов
```cpp
bool CUIRenderer::Initialize(ID3D12Device* device, ID3D12GraphicsCommandList* commandList, f32 width, f32 height) {
    // TODO:
    // 1. Создать root signature для UI шейдеров
    // 2. Создать pipeline state objects (PSO) для:
    //    - Filled rectangles
    //    - Textured rectangles
    //    - Text rendering
    //    - Gradients
    //    - Rounded corners
    // 3. Создать vertex/index buffers (dynamic)
    // 4. Создать constant buffers для:
    //    - Screen size
    //    - Transform matrices
    //    - Colors/effects
    // 5. Загрузить и скомпилировать шейдеры
    // 6. Настроить DirectWrite для текста (опционально)
}
```

#### B. Шейдеры для UI
Нужно создать файлы шейдеров:
- `UIShaders.hlsl` - базовые шейдеры для прямоугольников
- `UITextShaders.hlsl` - шейдеры для текста
- `UIEffectsShaders.hlsl` - шейдеры для эффектов (blur, shadow)

#### C. Рендеринг примитивов
```cpp
void DrawRect(const Rect2D& rect, const Color& color) {
    // TODO:
    // 1. Добавить вершины в batch buffer
    // 2. Если buffer полон - flush
    // 3. Применить transform stack
    // 4. Применить clip rect
}

void DrawText(const std::string& text, ...) {
    // TODO:
    // 1. Использовать DirectWrite для создания текстовых атласов
    // 2. Или использовать SDF (Signed Distance Field) текст
    // 3. Рендерить как textured quads
}

void DrawRoundedRect(...) {
    // TODO:
    // 1. Генерировать вершины для скругленных углов
    // 2. Или использовать pixel shader с distance functions
}
```

#### D. Batch rendering система
```cpp
// Нужно реализовать эффективный batching:
struct UIBatch {
    std::vector<UIVertex> vertices;
    std::vector<uint16_t> indices;
    ID3D12Resource* texture;
    BlendMode blendMode;
};

void FlushBatch() {
    // 1. Upload vertices/indices to GPU
    // 2. Set PSO
    // 3. Set root signature parameters
    // 4. Draw indexed
    // 5. Clear batch
}
```

### 2. **Текстовый рендеринг**

**Варианты реализации:**

#### Вариант A: DirectWrite + D2D (гибридный подход)
- Использовать D2D для рендеринга текста в текстуру
- Затем использовать эту текстуру в DX12
- **Плюсы:** Высокое качество, поддержка всех шрифтов
- **Минусы:** Сложная интеграция, overhead

#### Вариант B: SDF (Signed Distance Field) текст
- Предварительно генерировать SDF атласы для шрифтов
- Рендерить через pixel shader
- **Плюсы:** Масштабируемость, эффекты (outline, shadow)
- **Минусы:** Требует preprocessing

#### Вариант C: Bitmap font atlas
- Простые текстовые атласы
- **Плюсы:** Простота, производительность
- **Минусы:** Фиксированный размер, плохое масштабирование

**Рекомендация:** Начать с варианта C для прототипа, затем перейти на B

### 3. **Интеграция с существующей системой**

#### A. GameMain.cpp
```cpp
// Уже правильно инициализирует DX12:
g_renderer = new DirectXRenderer();
g_renderer->Initialize(g_hWnd, g_screenWidth, g_screenHeight);

// UI инициализируется с DX12:
Panorama::CUIEngine::Instance().Initialize(
    g_renderer->GetDevice(), 
    g_renderer, 
    uiConfig
);
```

#### B. Render loop
```cpp
void Render() {
    g_renderer->BeginFrame();
    
    // Рендерим в swapchain
    g_renderer->BeginSwapchainPass(clearColor);
    
    // Рендерим game state (включая UI)
    Game::GameStateManager::Instance().Render();
    
    g_renderer->EndFrame();
    g_renderer->Present();
}
```

### 4. **Ресурсы и текстуры**

#### A. Texture loading система
```cpp
class TextureManager {
    // TODO:
    // 1. Загрузка PNG/JPG через WIC или stb_image
    // 2. Создание D3D12 textures
    // 3. Upload через upload heap
    // 4. Создание SRV descriptors
    // 5. Кэширование текстур
};
```

#### B. Font atlas система
```cpp
class FontAtlas {
    // TODO:
    // 1. Генерация bitmap атласов для шрифтов
    // 2. UV координаты для каждого символа
    // 3. Kerning информация
};
```

### 5. **Эффекты и продвинутые фичи**

#### A. Box shadows
```cpp
void DrawBoxShadow(...) {
    // TODO:
    // 1. Рендерить blur в offscreen texture
    // 2. Применить gaussian blur через compute shader
    // 3. Composite с основным UI
}
```

#### B. Blur эффекты
```cpp
// Нужен compute shader для blur:
// - Gaussian blur
// - Box blur
// - Dual kawase blur (более эффективный)
```

#### C. Градиенты
```cpp
void DrawGradientRect(...) {
    // TODO:
    // 1. Vertex colors для linear gradients
    // 2. Pixel shader для radial gradients
}
```

### 6. **Оптимизации**

#### A. Instancing
- Использовать instanced rendering для повторяющихся элементов
- Особенно полезно для списков, таблиц

#### B. Texture atlasing
- Объединять мелкие текстуры в атласы
- Уменьшить количество draw calls

#### C. Culling
- Не рендерить элементы вне экрана
- Не рендерить полностью прозрачные элементы

## 📋 Приоритетный план действий

### Фаза 1: Базовый рендеринг ✅ ЗАВЕРШЕНО
1. ✅ Убрать debug логирование из DrawRect
2. ✅ Создать базовые UI шейдеры (vertex + pixel)
3. ✅ Реализовать DrawRect с реальным рендерингом
4. ✅ Реализовать batch систему
5. ✅ Тестировать на простых прямоугольниках

**Результат:** UI рендерится! Видны панели, кнопки, фоны.

### Фаза 2: Текст ⏳ ТЕКУЩАЯ ЗАДАЧА (2-3 дня)
1. ⏳ Создать простой bitmap font atlas
2. ⏳ Реализовать DrawText с atlas
3. ⏳ Добавить поддержку разных размеров шрифтов
4. ⏳ Тестировать на UI с текстом

**Временное решение:** Placeholder индикаторы вместо текста

### Фаза 3: Продвинутые фичи (3-5 дней)
1. ⏳ Rounded rectangles
2. ⏳ Градиенты
3. ⏳ Текстуры и изображения
4. ⏳ Clipping
5. ⏳ Transforms

### Фаза 4: Эффекты (опционально, 2-3 дня)
1. ⏳ Box shadows
2. ⏳ Blur эффекты
3. ⏳ Opacity/blending

### Фаза 5: Оптимизация (1-2 дня)
1. ⏳ Profiling
2. ⏳ Batching оптимизации
3. ⏳ Culling
4. ⏳ Texture atlasing

## 🔧 Технические детали

### Root Signature для UI
```cpp
// Пример root signature:
// b0: ConstantBuffer (screen size, transform)
// t0: Texture2D (для текстурированных quads)
// s0: SamplerState
```

### Vertex Format
```cpp
struct UIVertex {
    float x, y;           // Position
    float u, v;           // UV
    float r, g, b, a;     // Color
};
```

### Pipeline States
Нужно минимум 3 PSO:
1. **Solid color** - для прямоугольников без текстур
2. **Textured** - для изображений и текста
3. **Gradient** - для градиентов (или через vertex colors)

## 📚 Полезные ресурсы

1. **DirectX 12 UI rendering:**
   - ImGui DX12 backend (reference)
   - Dear ImGui renderer implementation

2. **Text rendering:**
   - DirectWrite + D2D integration
   - SDF text rendering papers
   - msdfgen library

3. **Batching:**
   - Sprite batching techniques
   - Draw call reduction strategies

## ⚠️ Известные проблемы

1. **Логирование:** ✅ ИСПРАВЛЕНО - убрали избыточное логирование
2. **Stub реализация:** Все методы DrawXXX - заглушки
3. **Нет шейдеров:** Нужно создать HLSL файлы
4. **Нет текстового рендеринга:** Критично для UI

## 🎯 Следующий шаг

**Начать с Фазы 1, пункт 2:** Создать базовые UI шейдеры и реализовать DrawRect с реальным рендерингом.
