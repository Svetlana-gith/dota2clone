# Улучшения системы крипов

## 📋 Обзор изменений

Система крипов была значительно доработана для более реалистичного поведения в MOBA-игре.

## ✅ Реализованные улучшения

### 1. Исправлена система атаки
**Проблема**: Использовался непрерывный урон (`damage * deltaTime * attackSpeed`), что давало неправильную механику боя.

**Решение**: Реализованы дискретные атаки с системой кулдауна:
- Атаки происходят только когда `attackCooldown <= 0`
- После атаки устанавливается кулдаун: `1.0f / attackSpeed`
- Урон наносится мгновенно, а не постепенно

### 2. Удаление мертвых крипов
**Проблема**: Мертвые крипы просто скрывались, но оставались в памяти навсегда.

**Решение**: 
- Добавлен `deathTime` и `deathDelay` в `CreepComponent`
- Мертвые крипы удаляются через 2 секунды после смерти
- Функция `cleanupDeadCreeps()` вызывается каждый кадр

### 3. Ограничение количества крипов
**Проблема**: Не было ограничений, крипы могли спавниться бесконечно.

**Решение**:
- Максимум 50 крипов на команду (`maxCreepsPerTeam_`)
- Максимум 20 крипов от одного спавн-поинта (`maxCreepsPerSpawn_`)
- Проверка лимитов перед каждым спавном

### 4. Система здоровья для башен/зданий
**Проблема**: Башни и здания не имели здоровья, крипы не могли их атаковать.

**Решение**:
- Добавлен `HealthComponent` с полями:
  - `maxHealth`, `currentHealth`
  - `armor`, `magicResistance`
  - `isDead`
- Башни: 1600 HP, 10 брони
- Здания: 2500 HP, 15 брони
- Автоматическое добавление при создании башен/зданий

### 5. Расчет урона с учетом брони
**Проблема**: Броня не учитывалась при расчете урона.

**Решение**:
- Реализована формула Dota 2-style:
  ```
  damageReduction = (armor * 0.06) / (1 + armor * 0.06)
  actualDamage = baseDamage * (1 - damageReduction)
  ```
- Поддержка отрицательной брони (увеличение урона)
- Функция `calculateDamage()` используется во всех атаках

### 6. Улучшенный поиск целей
**Проблема**: Простой поиск ближайшей цели без приоритетов.

**Решение**:
- Система приоритетов:
  - Крипы, атакующие нас: приоритет 2.0
  - Башни: приоритет 1.5
  - Обычные цели: приоритет 1.0
- Оценка цели: `score = priority / (distance + 1.0)`
- Выбирается цель с максимальным score
- Учитывается аггрессия (враги, атакующие нас, имеют приоритет)

### 7. Система аггрессии
**Проблема**: Крипы не реагировали на атаки.

**Решение**:
- При поиске целей проверяется `targetCreep.targetEntity == creepEntity`
- Агрессивные враги получают приоритет 2.0
- Крипы автоматически переключаются на атакующих их врагов

## 🔧 Технические детали

### Новые компоненты

#### HealthComponent
```cpp
struct HealthComponent {
    f32 maxHealth = 1000.0f;
    f32 currentHealth = 1000.0f;
    f32 armor = 5.0f;
    f32 magicResistance = 0.0f;
    bool isDead = false;
};
```

#### Обновленный CreepComponent
```cpp
struct CreepComponent {
    // ... существующие поля ...
    f32 deathTime = 0.0f;      // Время смерти
    f32 deathDelay = 2.0f;     // Задержка перед удалением
};
```

### Новые методы CreepSystem

- `cleanupDeadCreeps(f32 deltaTime)` - удаление мертвых крипов
- `calculateDamage(f32 baseDamage, f32 targetArmor)` - расчет урона с броней
- `countActiveCreeps(i32 teamId)` - подсчет активных крипов команды
- `countCreepsFromSpawn(Entity spawnPoint)` - подсчет крипов от спавна

### Улучшенные методы

- `attackTarget()` - дискретные атаки вместо непрерывного урона
- `findTarget()` - система приоритетов и аггрессии
- `spawnCreeps()` - проверка лимитов перед спавном

## 🎮 Игровая механика

### Боевая система
- **Дискретные атаки**: Крипы атакуют с интервалом, зависящим от `attackSpeed`
- **Броня**: Снижает урон по формуле Dota 2
- **Приоритеты**: Агрессивные враги и башни имеют приоритет

### Управление крипами
- **Лимиты**: Максимум 50 крипов на команду, 20 от спавна
- **Очистка**: Мертвые крипы удаляются через 2 секунды
- **Аггрессия**: Крипы реагируют на атаки и переключаются на агрессоров

### Здоровье структур
- **Башни**: 1600 HP, 10 брони
- **Здания**: 2500 HP, 15 брони
- **Крипы**: Могут атаковать и уничтожать башни/здания

## 📊 Производительность

- Эффективный поиск целей с ранним выходом
- Пакетное удаление мертвых крипов (один раз за кадр)
- Минимальные проверки лимитов (только при спавне)

## 🔮 Будущие улучшения

1. Система опыта и золота за убийства
2. Баффы/дебаффы для крипов
3. Правильное следование по линиям (NavMesh)
4. Коллизии между крипами
5. Анимации атаки и смерти
6. Система таунта (принудительная атака)
