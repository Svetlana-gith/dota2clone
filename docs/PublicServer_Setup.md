# Public Server Setup Guide

## 🌐 Как сделать DedicatedServer публичным

Ваш сервер уже готов к публичному доступу! Нужно только настроить роутер.

---

## 📊 Ваши данные

- **Локальный IP:** `192.168.87.104`
- **Публичный IP:** `83.99.137.79`
- **Порт сервера:** `27015` (UDP)

---

## 🔧 Настройка Port Forwarding на роутере

### Шаг 1: Войти в роутер

1. Откройте браузер
2. Перейдите по адресу: `http://192.168.87.1` (или `192.168.1.1`)
3. Введите логин/пароль (обычно `admin`/`admin` или написано на роутере)

### Шаг 2: Найти Port Forwarding

Ищите раздел с названием:
- **Port Forwarding**
- **Virtual Server**
- **NAT**
- **Переадресация портов**

### Шаг 3: Добавить правило

Создайте новое правило со следующими параметрами:

```
Service Name:     DedicatedServer
Protocol:         UDP
External Port:    27015
Internal IP:      192.168.87.104
Internal Port:    27015
Status:           Enabled
```

**Пример для разных роутеров:**

#### TP-Link
```
Forwarding → Virtual Servers → Add New
- Service Port: 27015
- IP Address: 192.168.87.104
- Internal Port: 27015
- Protocol: UDP
- Status: Enabled
```

#### ASUS
```
WAN → Virtual Server/Port Forwarding
- Service Name: DedicatedServer
- Port Range: 27015
- Local IP: 192.168.87.104
- Local Port: 27015
- Protocol: UDP
```

#### D-Link
```
Advanced → Port Forwarding
- Name: DedicatedServer
- Public Port: 27015
- Private IP: 192.168.87.104
- Private Port: 27015
- Protocol: UDP
```

### Шаг 4: Сохранить и перезагрузить роутер

---

## 🔥 Настройка Windows Firewall

### Вариант 1: Через GUI (проще)

1. Откройте **Windows Defender Firewall**
2. Нажмите **"Дополнительные параметры"**
3. Выберите **"Правила для входящих подключений"**
4. Нажмите **"Создать правило"**
5. Выберите **"Для порта"** → Далее
6. Выберите **UDP**, укажите порт **27015** → Далее
7. Выберите **"Разрешить подключение"** → Далее
8. Отметьте все профили → Далее
9. Имя: **"DedicatedServer UDP"** → Готово

### Вариант 2: Через PowerShell (от администратора)

```powershell
# Запустите PowerShell от администратора
New-NetFirewallRule -DisplayName "DedicatedServer UDP" -Direction Inbound -Protocol UDP -LocalPort 27015 -Action Allow
```

---

## ✅ Проверка настройки

### 1. Запустить сервер

```bash
.\build\bin\Release\DedicatedServer.exe
```

Вы должны увидеть:
```
[info] === Dedicated Server Ready ===
[info] Listening on port 27015
```

### 2. Проверить порт локально

```bash
netstat -an -p UDP | findstr "27015"
```

Должно быть:
```
UDP    0.0.0.0:27015          *:*
```

### 3. Проверить порт извне

Попросите друга подключиться к вашему серверу:

```cpp
// В Game.exe
networkClient->connect("83.99.137.79", 27015);
```

Или используйте онлайн-сервис:
- https://www.yougetsignal.com/tools/open-ports/
- Введите IP: `83.99.137.79`
- Введите порт: `27015`
- Выберите протокол: `UDP`

---

## 🎮 Подключение клиентов

### Локальные клиенты (в вашей сети)

```cpp
networkClient->connect("192.168.87.104", 27015);
```

### Внешние клиенты (из интернета)

```cpp
networkClient->connect("83.99.137.79", 27015);
```

---

## 🔒 Безопасность

### ⚠️ Важно!

1. **Не открывайте порты без необходимости**
   - Открытые порты = потенциальная уязвимость

2. **Используйте только UDP порт 27015**
   - Не открывайте весь диапазон портов

3. **Добавьте аутентификацию**
   - Пароль для подключения к серверу
   - Whitelist IP адресов

4. **Мониторинг**
   - Следите за логами сервера
   - Блокируйте подозрительные IP

### Рекомендации:

```cpp
// В NetworkServer.cpp - добавить проверку пароля
void NetworkServer::handleConnectionRequest(const NetworkAddress& sender) {
    // TODO: Проверить пароль в пакете
    // TODO: Проверить IP в whitelist
    // TODO: Rate limiting (защита от DDoS)
}
```

---

## 🌍 Альтернативы Port Forwarding

Если у вас нет доступа к роутеру или провайдер использует CGNAT:

### 1. **VPN (Hamachi, ZeroTier)**
- Создает виртуальную локальную сеть
- Не нужен Port Forwarding
- Бесплатно для небольших групп

### 2. **Облачный сервер (VPS)**
- Арендовать сервер (AWS, DigitalOcean, Hetzner)
- Публичный IP из коробки
- ~$5-10/месяц

### 3. **Tunneling (ngrok, Cloudflare Tunnel)**
- Пробрасывает локальный порт в интернет
- Не нужен Port Forwarding
- Бесплатные тарифы доступны

---

## 🧪 Тестирование

### Локальный тест (на одном ПК)

1. Запустить `DedicatedServer.exe`
2. Запустить `Game.exe`
3. Подключиться к `127.0.0.1:27015`

### Тест в локальной сети

1. Запустить `DedicatedServer.exe` на ПК #1
2. Запустить `Game.exe` на ПК #2
3. Подключиться к `192.168.87.104:27015`

### Тест через интернет

1. Запустить `DedicatedServer.exe`
2. Попросить друга запустить `Game.exe`
3. Друг подключается к `83.99.137.79:27015`

---

## 📊 Мониторинг

### Проверка подключений

```bash
# Сколько клиентов подключено
netstat -an -p UDP | findstr "27015"
```

### Логи сервера

Сервер выводит статистику каждые 10 секунд:
```
[info] === Server Stats ===
[info]   Tick Rate: 30.0 Hz (target: 30)
[info]   Clients: 2
[info]   Entities: 45
[info]   Game Time: 123.4s
```

---

## 🚀 Production Deployment

Для серьезного деплоя рекомендуется:

### 1. Linux VPS (Ubuntu/Debian)

```bash
# Установить зависимости
sudo apt update
sudo apt install build-essential cmake

# Скомпилировать сервер
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --target DedicatedServer

# Запустить как сервис
sudo systemctl enable dedicatedserver
sudo systemctl start dedicatedserver
```

### 2. Docker контейнер

```dockerfile
FROM ubuntu:22.04
RUN apt update && apt install -y libstdc++6
COPY DedicatedServer /app/
EXPOSE 27015/udp
CMD ["/app/DedicatedServer"]
```

### 3. Kubernetes (для масштабирования)

```yaml
apiVersion: v1
kind: Service
metadata:
  name: dedicatedserver
spec:
  type: LoadBalancer
  ports:
  - port: 27015
    protocol: UDP
```

---

## 📝 Checklist

- [ ] Узнал локальный IP (`192.168.87.104`)
- [ ] Узнал публичный IP (`83.99.137.79`)
- [ ] Настроил Port Forwarding на роутере
- [ ] Добавил правило в Windows Firewall
- [ ] Запустил DedicatedServer.exe
- [ ] Проверил порт через netstat
- [ ] Протестировал подключение локально
- [ ] Протестировал подключение извне
- [ ] Добавил аутентификацию (опционально)
- [ ] Настроил мониторинг (опционально)

---

## 🆘 Troubleshooting

### Проблема: "Connection timeout"

**Решение:**
1. Проверьте, что сервер запущен
2. Проверьте Port Forwarding на роутере
3. Проверьте Windows Firewall
4. Проверьте, что используете правильный IP

### Проблема: "Port already in use"

**Решение:**
```bash
# Найти процесс, использующий порт
netstat -ano | findstr "27015"

# Убить процесс (замените PID)
taskkill /F /PID <PID>
```

### Проблема: "Cannot bind socket"

**Решение:**
- Запустите от администратора
- Проверьте, что порт не занят другой программой

---

**Готово!** Теперь ваш сервер доступен из интернета по адресу `83.99.137.79:27015` 🎉
